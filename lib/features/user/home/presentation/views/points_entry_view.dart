import 'package:flutter/material.dart';

import 'widgets/points_entry_view_body.dart';

class PointsEntryView extends StatelessWidget {
  const PointsEntryView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body:PointsEntryViewBody());
  }
}
